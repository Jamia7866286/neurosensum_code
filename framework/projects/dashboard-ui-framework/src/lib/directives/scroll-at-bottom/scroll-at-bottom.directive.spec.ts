import { ScrollAtBottomDirective } from './scroll-at-bottom.directive';

describe('ScrollAtBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollAtBottomDirective();
    expect(directive).toBeTruthy();
  });
});
