import { Injectable, Optional } from '@angular/core';
import { FrameworkConfig } from '../../models/model';

@Injectable({
  providedIn: 'root'
})
export class ConfigService extends FrameworkConfig {
}
